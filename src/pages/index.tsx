import { DocumentContext } from "next/document";

export default function Home(value: DocumentContext) {
  if (!value) {
    return "Loading...";
  }

  return (
    <>
      {/* <Body /> */}
      ow w
    </>
  );
}
